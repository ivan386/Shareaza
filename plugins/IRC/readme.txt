It's a basic sketch of IRC plugin.

ToDo: migrate IRC code from RazaIRC branch.
Compile Shareaza.idl separately (Shareaza code won't compile, so backup the old version before). We need only generated files.