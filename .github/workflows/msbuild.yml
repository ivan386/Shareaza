name: MSBuild

on:
  push:
    branches: [ "4-add-support-for-github-actions" ]
  pull_request:
    branches: [ "4-add-support-for-github-actions" ]

env:
  # Path to the solution file relative to the root of the project.
  SOLUTION_FILE_PATH: vc143/Shareaza.sln
  RELEASEBUILD_FILE_PATH: vc143/ReleaseBuild.props

  # Configuration type to build.
  # You can convert this to a build matrix if you need coverage of multiple configuration types.
  # https://docs.github.com/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
  BUILD_CONFIGURATION: Release

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Restore NuGet packages (zlibwapi)
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: nuget restore zlibwapi/zlibwapi_143.vcxproj

    - name: Restore NuGet packages (Shareaza)
      working-directory: ${{env.GITHUB_WORKSPACE}}
      run: nuget restore ${{env.SOLUTION_FILE_PATH}}

    - name: Build
      working-directory: ${{env.GITHUB_WORKSPACE}}
      # Add additional options to the MSBuild command line here (like platform or verbosity level).
      # See https://docs.microsoft.com/visualstudio/msbuild/msbuild-command-line-reference
      run: msbuild ${{env.SOLUTION_FILE_PATH}} /nologo /v:m /t:Rebuild /p:ForceImportBeforeCppTargets=${{env.RELEASEBUILD_FILE_PATH}} /p:Configuration=${{env.BUILD_CONFIGURATION}} /p:Platform=Win32 /fl /flp:Summary